import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function AdminTasksPage() {
    return (
        <PlaceholderPage
            title="Tasks"
            description="Create, reorder, and manage the learning path in Milestone 4."
        />
    );
}