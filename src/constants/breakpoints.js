
/**
 * Media query breakpoints used for responsive UI decisions.
 * Matches Tailwind's `md:` breakpoint (≥768px).
 * 
 * @type {{ MD: string }}
 */
export const BREAKPOINTS = {
    MD: "(min-width: 768px)",
};